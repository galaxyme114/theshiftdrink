<% if can?(:administrate, Media) %>
    <div class="caption">
		<p class="no-margin">
			<%= link_to('Edit', edit_medium_path(@issue, @article, media), :class => 'label label-default') %>

			<%= link_to('Remove', medium_path(@issue, @article, media), method: :delete,
					:class => 'label label-danger', data: { confirm: 'Are you sure you want to remove this page?' }) %>
		</p>
	</div>
<% end %>
